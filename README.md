Emotion Recognition from Facial Images (Mini Project)

Steps:
1. Install requirements:
   pip install tensorflow opencv-python numpy

2. Add dataset folders inside 'dataset'
3. Run: python train_model.py
4. Run: python live_emotion.py
Press ESC to exit.
